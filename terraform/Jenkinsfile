
stage 'Create Stack'

node("master") {
    checkout scm
    dir("terraform/") {
        sh "ls -al"
        sh "chmod 777 ./bin/create-stack.sh"
        sh "ls -al ./bin"
        sh "./bin/create-stack.sh --destroy -v pilot '${env.BRANCH_NAME}'"
    }
}
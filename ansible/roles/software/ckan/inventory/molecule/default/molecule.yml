---
dependency:
  name: galaxy
driver:
  name: docker
lint:
  name: yamllint
  enabled: false # to fix
platforms:
  - name: ckan-inventory-app-trusty
    image: ubuntu:trusty
  - name: ckan-inventory-app-bionic
    image: ubuntu:bionic
provisioner:
  name: ansible
  lint:
    name: ansible-lint
scenario:
  name: default
  # to fix
  test_sequence:
    - lint
    - destroy
    - dependency
    - syntax
    - create
    - prepare
    - converge
    # - idempotence
    - side_effect
    - verify
    - destroy  
verifier:
  name: testinfra
  lint:
    name: flake8
